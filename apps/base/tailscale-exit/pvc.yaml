apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tailscale-exit-state
  namespace: tailscale
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: unraid-nfs
  resources:
    requests:
      storage: 1Gi
