apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-paperless
  namespace: paperless
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloudflared-paperless
  template:
    metadata:
      labels:
        app: cloudflared-paperless
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:2026.8.2
          args:
            - tunnel
            - --config
            - /etc/cloudflared/config/config.yaml
            - run
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            failureThreshold: 3
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/cloudflared/config
              readOnly: true
            - name: creds
              mountPath: /etc/cloudflared/creds
              readOnly: true
      volumes:
        - name: creds
          secret:
            secretName: paperless-tunnel-credentials
        - name: config
          configMap:
            name: cloudflared-paperless
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-paperless
  namespace: paperless
data:
  config.yaml: |
    tunnel: paperless
    credentials-file: /etc/cloudflared/creds/credentials.json
    metrics: 0.0.0.0:2000
    no-autoupdate: true
    ingress:
      - hostname: paperless.nohermosa.com
        service: http://paperless-web.paperless.svc.cluster.local:8000
      - service: http_status:404
