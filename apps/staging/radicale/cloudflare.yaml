apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-radicale
  namespace: radicale
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloudflared-radicale
  template:
    metadata:
      labels:
        app: cloudflared-radicale
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:2026.7.3
          args:
            - tunnel
            - --config
            - /etc/cloudflared/config/config.yaml
            - run
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            failureThreshold: 3
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/cloudflared/config
              readOnly: true
            - name: creds
              mountPath: /etc/cloudflared/creds
              readOnly: true
      volumes:
        - name: creds
          secret:
            secretName: radicale-tunnel-credentials
        - name: config
          configMap:
            name: cloudflared-radicale
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-radicale
  namespace: radicale
data:
  config.yaml: |
    tunnel: radicale
    credentials-file: /etc/cloudflared/creds/credentials.json
    metrics: 0.0.0.0:2000
    no-autoupdate: true
    ingress:
      - hostname: radicale.nohermosa.com
        service: http://radicale.radicale.svc.cluster.local:5232
      - service: http_status:404
