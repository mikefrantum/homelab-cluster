apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-jellyseerr
  namespace: media
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloudflared-jellyseerr
  template:
    metadata:
      labels:
        app: cloudflared-jellyseerr
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:2026.8.0
          args:
            - tunnel
            - --config
            - /etc/cloudflared/config/config.yaml
            - run
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            failureThreshold: 3
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/cloudflared/config
              readOnly: true
            - name: creds
              mountPath: /etc/cloudflared/creds
              readOnly: true
      volumes:
        - name: creds
          secret:
            secretName: jellyseerr-tunnel-credentials
        - name: config
          configMap:
            name: cloudflared-jellyseerr
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-jellyseerr
  namespace: media
data:
  config.yaml: |
    tunnel: jelly
    credentials-file: /etc/cloudflared/creds/credentials.json
    metrics: 0.0.0.0:2000
    no-autoupdate: true
    ingress:
      - hostname: jelly.nohermosa.com
        service: http://jellyseerr.media.svc.cluster.local:5055
      - service: http_status:404
