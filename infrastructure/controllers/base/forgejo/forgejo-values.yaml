apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: forgejo
  namespace: forgejo
spec:
  values:
    # Example staging overrides (adjust as needed)
    persistence:
      size: 10Gi

